{
  "name": "kolkhoz",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "play": "npx esbuild scripts/play.js --bundle --platform=node --format=esm --outfile=scripts/play.bundle.js && node scripts/play.bundle.js"
  },
  "dependencies": {
    "bgio-effects": "^0.7.1",
    "boardgame.io": "^0.50.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "esbuild": "^0.27.2",
    "vite": "^5.0.12",
    "vitest": "^3.2.4"
  }
}
