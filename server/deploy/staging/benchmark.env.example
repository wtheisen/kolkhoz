# Use a dedicated benchmark database or project, never the live application database.
DATABASE_URL=postgresql://benchmark_user:REPLACE@postgres.example:5432/kolkhoz_benchmark?sslmode=require
# Use a dedicated Redis instance or a database number unused by live traffic.
REDIS_URL=rediss://:REPLACE@redis.example:6379/15
BENCHMARK_PORT=19080
